# План миграции на модель Публичной Оферты

## 1. ЧТО УДАЛИТЬ (Очистка кода)

### 1.1. Модели и поля, которые больше не нужны

#### Модель `Contract` (частично)
- ❌ **Удалить полностью**: Модель `Contract` и все связанные модели
  - `ContractApprovalHistory` - история согласования контрактов
  - `ContractCommission` - комиссии по контрактам (если отдельная модель)
  - `ContractDiscount` - скидки по контрактам (если отдельная модель)
- ⚠️ **Оставить временно**: Поля в `Provider`, связанные с контрактами, для миграции данных
- 📝 **Примечание**: Нужно проверить, используются ли контракты в биллинге/инвойсах

#### Поля в `Provider`
- ❌ **Удалить**: Связь с контрактами через `related_name='contracts'`
- ⚠️ **Оставить**: `tax_id`, `registration_number` (будут расширены)

#### Процесс создания контрактов
- ❌ **Удалить**: 
  - API endpoints для создания/редактирования контрактов
  - Админка для контрактов (`ContractAdmin`)
  - Сериализаторы контрактов (`ContractSerializer`, `ContractCreateSerializer`)
  - Диаграммы: `5. PROVIDER_CONTRACT_CREATION.puml`, `6. PROVIDER_CONTRACT_APPROVAL.puml`
  - Сигналы, связанные с контрактами

#### Статусы контрактов
- ❌ **Удалить**: Все статусы контрактов (`draft`, `pending_approval`, `active`, `rejected`, `suspended`, `terminated`)
- ❌ **Удалить**: Поля `status`, `submitted_for_approval_at`, `approved_by`, `approved_at`, `rejection_reason` из `Contract`

### 1.2. Процессы, которые больше не нужны

#### Ручное создание контрактов
- ❌ **Удалить**: Процесс создания контракта биллинг-менеджером
- ❌ **Удалить**: Процесс согласования контракта системным админом
- ❌ **Удалить**: Процесс отправки оферты провайдеру на подпись

#### Проверка контрактов
- ❌ **Удалить**: Логику проверки наличия активного контракта перед выводом услуг на UI
- ❌ **Удалить**: Блокировки, связанные с отсутствием контракта

### 1.3. Файлы для удаления

```
PetsCare/billing/models.py:
  - class Contract (полностью)
  - class ContractApprovalHistory (полностью)
  - class ContractCommission (если есть)
  - class ContractDiscount (если есть)

PetsCare/billing/admin.py:
  - class ContractAdmin (полностью)
  - class ContractApprovalHistoryAdmin (если есть)

PetsCare/billing/serializers.py:
  - class ContractSerializer
  - class ContractCreateSerializer
  - class ContractApprovalHistorySerializer (если есть)

PetsCare/billing/api_views.py:
  - ContractViewSet (если есть)

PetsCare/providers/docs/diagrams/:
  - 5. PROVIDER_CONTRACT_CREATION.puml
  - 6. PROVIDER_CONTRACT_APPROVAL.puml
```

---

## 2. ЧТО ДОБАВИТЬ

### 2.1. Новые модели

#### Модель `PublicOffer` (замена `Contract`)
```python
class PublicOffer(models.Model):
    """
    Публичная оферта - единый договор для всех провайдеров.
    """
    version = models.CharField(max_length=20)  # Версия оферты (1.0, 1.1, etc.)
    title = models.CharField(max_length=200)
    content = models.TextField()  # Текст оферты (основной текст)
    effective_date = models.DateField()  # Дата вступления в силу
    is_active = models.BooleanField(default=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    # Связь с региональными дополнениями (ManyToMany)
    regional_addendums = models.ManyToManyField(
        'RegionalAddendum',
        related_name='offers',
        blank=True,
        help_text=_('Regional addendums that apply to this offer')
    )
```

#### Модель `RegionalAddendum` (региональные дополнения)
```python
class RegionalAddendum(models.Model):
    """
    Региональное дополнение к публичной оферте.
    Применяется для конкретных стран/регионов (ЕС, РФ, УА и т.д.).
    """
    REGION_CHOICES = [
        ('EU', _('European Union')),
        ('EEA', _('European Economic Area')),
        ('RU', _('Russian Federation')),
        ('UA', _('Ukraine')),
        ('US', _('United States')),
        # Можно добавить другие регионы
    ]
    
    region_code = models.CharField(
        max_length=10,
        choices=REGION_CHOICES,
        unique=True,
        help_text=_('Region code (EU, RU, UA, etc.)')
    )
    region_name = models.CharField(
        max_length=100,
        help_text=_('Human-readable region name')
    )
    version = models.CharField(
        max_length=20,
        help_text=_('Version of the addendum (1.0, 1.1, etc.)')
    )
    title = models.CharField(
        max_length=200,
        help_text=_('Title of the regional addendum')
    )
    content = models.TextField(
        help_text=_('Content of the regional addendum')
    )
    effective_date = models.DateField(
        help_text=_('Date when this addendum becomes effective')
    )
    is_active = models.BooleanField(
        default=True,
        help_text=_('Whether this addendum is currently active')
    )
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        verbose_name = _('Regional Addendum')
        verbose_name_plural = _('Regional Addendums')
        unique_together = ['region_code', 'version']
        ordering = ['region_code', '-effective_date']
    
    def __str__(self):
        return f"{self.region_name} - {self.version}"
```

#### Модель `ProviderOfferAcceptance` (акцепт оферты)
```python
class ProviderOfferAcceptance(models.Model):
    """
    Акцепт публичной оферты провайдером.
    """
    provider = models.ForeignKey(Provider, on_delete=models.CASCADE)
    offer = models.ForeignKey(PublicOffer, on_delete=models.PROTECT)
    accepted_at = models.DateTimeField(auto_now_add=True)
    accepted_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)
    ip_address = models.GenericIPAddressField()  # Для аудита
    user_agent = models.TextField()  # Для аудита
    is_active = models.BooleanField(default=True)  # Если оферта обновлена, старые акцепты неактивны
```

#### Модель `SideLetter` (доп. соглашения для крупных клиентов)
```python
class SideLetter(models.Model):
    """
    Дополнительное соглашение (Side Letter) для крупных клиентов.
    Изменяет отдельные пункты публичной оферты.
    """
    provider = models.ForeignKey(Provider, on_delete=models.CASCADE)
    offer = models.ForeignKey(PublicOffer, on_delete=models.PROTECT)
    modified_clauses = models.JSONField()  # {"5.1": "Комиссия 5% вместо 10%"}
    document = models.FileField(upload_to='side_letters/%Y/%m/%d/')
    signed_at = models.DateTimeField(null=True, blank=True)
    created_by = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)
    created_at = models.DateTimeField(auto_now_add=True)
    is_active = models.BooleanField(default=True)
```

### 2.2. Расширение модели `Provider`

#### Новые поля реквизитов

**Основной блок:**
```python
# Страна регистрации (главный триггер)
country = models.CharField(max_length=100)  # Dropdown: список стран
is_eu = models.BooleanField(default=False)  # Системное поле (автоматически)

# Тип организации
organization_type = models.CharField(
    max_length=50,
    choices=ORGANIZATION_TYPE_CHOICES  # ИП, ООО, Corp, LLC, etc.
)

# Юридическое название (уже есть как name)
legal_name = models.CharField(max_length=200)  # Дублирует name, но для ясности

# ФИО Директора
director_name = models.CharField(max_length=200)
```

**Юридический блок:**
```python
# Регистрационный номер (уже есть registration_number)
# Юридический адрес (уже есть structured_address)
```

**Налоговый блок:**
```python
# Tax ID (уже есть tax_id)
# КПП (только для РФ, ООО)
kpp = models.CharField(max_length=20, null=True, blank=True)  # Только для РФ

# Плательщик НДС?
is_vat_payer = models.BooleanField(default=False)

# VAT Number (только для ЕС, если is_vat_payer=True)
vat_number = models.CharField(max_length=50, null=True, blank=True)
vat_verified = models.BooleanField(default=False)  # Результат проверки через VIES
vat_verification_date = models.DateTimeField(null=True, blank=True)
```

**Финансовые данные:**
```python
# Валюта счета
invoice_currency = models.ForeignKey(Currency, on_delete=models.PROTECT)

# IBAN (для ЕС/УА)
iban = models.CharField(max_length=34, null=True, blank=True)

# SWIFT / BIC
swift_bic = models.CharField(max_length=11)

# Название банка
bank_name = models.CharField(max_length=200)
```

### 2.3. Новые сервисы и утилиты

#### Сервис проверки VAT через VIES API
```python
# PetsCare/billing/services/vat_verification.py
class VATVerificationService:
    """
    Сервис для проверки VAT номеров через VIES API (ЕС).
    """
    @staticmethod
    def verify_vat_number(vat_number: str, country_code: str) -> dict:
        """
        Проверяет VAT номер через VIES API.
        
        Returns:
            {
                'valid': bool,
                'name': str,  # Название компании из реестра
                'address': str,  # Адрес из реестра
                'request_date': str,
                'error': str  # Если есть ошибка
            }
        """
        pass
```

#### Сервис генерации оферты
```python
# PetsCare/billing/services/offer_generator.py
class OfferGeneratorService:
    """
    Сервис для генерации персонализированной оферты для провайдера.
    """
    @staticmethod
    def generate_offer_for_provider(provider: Provider, offer: PublicOffer) -> str:
        """
        Генерирует текст оферты с подстановкой данных провайдера.
        Включает основной текст оферты + региональные дополнения (если применимы).
        
        Args:
            provider: Provider объект
            offer: PublicOffer объект
            
        Returns:
            str: Полный текст оферты (HTML или plain text)
        """
        # 1. Получить основной текст оферты
        # 2. Определить регион провайдера (по country)
        # 3. Получить активные региональные дополнения для этого региона
        # 4. Объединить основной текст + региональные дополнения
        # 5. Подставить данные провайдера (название, адрес, директор и т.д.)
        pass
    
    @staticmethod
    def get_applicable_addendums(provider: Provider, offer: PublicOffer) -> QuerySet:
        """
        Получает региональные дополнения, применимые к провайдеру.
        
        Args:
            provider: Provider объект
            offer: PublicOffer объект
            
        Returns:
            QuerySet: Региональные дополнения, применимые к провайдеру
        """
        # Определить регион по country провайдера
        # Если is_eu = True -> регион 'EU'
        # Если country = 'RU' -> регион 'RU'
        # Если country = 'UA' -> регион 'UA'
        # Вернуть активные дополнения для этого региона из offer.regional_addendums
        pass
```

### 2.4. Новые API endpoints

#### Для провайдеров
```python
# POST /api/providers/{id}/accept-offer/
# Принятие публичной оферты

# GET /api/providers/{id}/offer/
# Получение текста оферты для провайдера

# POST /api/providers/{id}/verify-vat/
# Проверка VAT номера через VIES
```

#### Для админов
```python
# GET /api/admin/offers/
# Список публичных оферт

# POST /api/admin/offers/
# Создание новой версии оферты

# POST /api/admin/providers/{id}/side-letter/
# Создание Side Letter для провайдера
```

### 2.5. Новые процессы

#### Процесс регистрации провайдера (новый флоу)
1. Регистрация пользователя (Email + Пароль)
2. Создание организации (базовые данные)
3. Заполнение реквизитов (с валидацией через реестры)
4. Принятие публичной оферты (автоматическое создание `ProviderOfferAcceptance`)
5. Активация провайдера (автоматически после принятия оферты)

#### Процесс проверки VAT (для ЕС)
1. Провайдер выбирает страну (ЕС)
2. Система помечает `is_eu = True`
3. Провайдер ставит галочку "Я плательщик НДС"
4. Провайдер вводит VAT Number
5. Система проверяет через VIES API
6. Если валидно - ставит `vat_verified = True`, `vat_exempt = True`
7. Если невалидно - показывает ошибку, НДС начисляется

---

## 3. ДИАГРАММЫ ФЛОУ (PlantUML)

### 3.1. Новые диаграммы для создания

#### `7. PROVIDER_REGISTRATION_WITH_OFFER.puml`
- Регистрация пользователя
- Создание организации
- Заполнение реквизитов
- Принятие публичной оферты
- Активация провайдера

#### `8. VAT_VERIFICATION_FLOW.puml`
- Выбор страны (ЕС)
- Ввод VAT Number
- Проверка через VIES API
- Обработка результата

#### `9. SIDE_LETTER_CREATION.puml`
- Создание Side Letter админом
- Привязка к провайдеру
- Загрузка документа
- Активация

### 3.2. Диаграммы для обновления

#### `1. PROVIDER_FORM_CREATION_SEQUENCE.puml`
- Добавить шаг принятия оферты
- Добавить шаг проверки VAT (для ЕС)

#### `2. PROVIDER_FORM_APPROVAL.puml`
- Убрать создание контракта
- Оставить только создание Provider и назначение биллинг-менеджера

#### `3. PROVIDER_REQUISITES_FILLING.puml`
- Расширить поля реквизитов
- Добавить проверку VAT (для ЕС)
- Добавить валидацию через реестры

---

## 4. ФОРМУЛИРОВКИ ДЛЯ ФУНКЦИОНАЛЬНОГО ДИЗАЙНА

### 4.1. Что вычистить из ФД

#### Раздел "Контракты"
- ❌ Удалить полностью раздел "Создание контрактов"
- ❌ Удалить раздел "Согласование контрактов"
- ❌ Удалить раздел "Отправка оферты провайдеру"
- ❌ Удалить раздел "Принятие оферты провайдером"
- ❌ Удалить статусы контрактов (`draft`, `pending_approval`, `active`, etc.)
- ❌ Удалить процесс создания контракта биллинг-менеджером
- ❌ Удалить процесс согласования контракта системным админом

#### Раздел "Провайдеры"
- ❌ Удалить проверку наличия активного контракта перед выводом услуг
- ❌ Удалить блокировки, связанные с отсутствием контракта
- ⚠️ Оставить процесс активации провайдера (но без привязки к контракту)

### 4.2. Что добавить/изменить в ФД

#### Новый раздел "Публичная оферта"

**4.2.1. Управление публичной офертой (для системных админов)**
- Создание новой версии оферты
- Редактирование текста оферты
- Установка даты вступления в силу
- Деактивация старых версий
- Привязка региональных дополнений к оферте (ManyToMany)

**4.2.1.1. Управление региональными дополнениями (для системных админов)**
- Создание региональных дополнений для разных регионов (ЕС, РФ, УА и т.д.)
- Версионирование региональных дополнений
- Редактирование текста дополнений
- Установка даты вступления в силу
- Деактивация старых версий
- Привязка дополнений к офертам

**4.2.2. Принятие оферты провайдером**
- Автоматическая генерация персонализированной оферты (с подстановкой данных провайдера)
- Автоматическое определение применимых региональных дополнений (по стране провайдера)
- Отображение полного текста оферты (основной текст + региональные дополнения) в личном кабинете
- Чекбокс "Принимаю условия оферты"
- Сохранение акцепта с IP-адресом и User-Agent для аудита
- Сохранение информации о принятых региональных дополнениях
- Автоматическая активация провайдера после принятия оферты

**4.2.3. Side Letter (доп. соглашения)**
- Создание Side Letter для крупных клиентов
- Указание измененных пунктов оферты
- Загрузка подписанного документа
- Привязка к провайдеру
- Активация/деактивация Side Letter

#### Обновление раздела "Реквизиты провайдера"

**4.2.4. Расширенные реквизиты**
- Выбор страны регистрации (главный триггер)
- Автоматическое определение принадлежности к ЕС (`is_eu`)
- Выбор типа организации (ИП, ООО, Corp, LLC, etc.)
- Ввод ФИО директора
- Ввод регистрационного номера (с валидацией через реестры)
- Ввод Tax ID / ИНН (с валидацией через реестры)
- КПП (только для РФ, ООО) - скрывается для других стран
- Чекбокс "Я плательщик НДС"
- VAT Number (только для ЕС, если плательщик НДС) - с проверкой через VIES
- Валюта счета
- IBAN (для ЕС/УА) или номер расчетного счета (для РФ)
- SWIFT / BIC
- Название банка

**4.2.5. Валидация реквизитов**
- Проверка VAT Number через VIES API (для ЕС)
- Проверка ИНН через реестры (для РФ) - если есть API
- Проверка регистрационного номера через реестры (для разных стран)
- Автоматическое скрытие/показ полей в зависимости от страны
- Валидация формата IBAN
- Валидация формата SWIFT/BIC

#### Обновление раздела "Регистрация провайдера"

**4.2.6. Новый процесс регистрации**
1. Регистрация пользователя (Email + Пароль)
2. Создание организации (название, адрес, контакты)
3. Заполнение реквизитов (с валидацией)
4. Принятие публичной оферты (обязательно)
5. Автоматическая активация провайдера

**4.2.7. Упрощение процесса одобрения**
- Убрать создание контракта при одобрении заявки
- Оставить только создание Provider и назначение биллинг-менеджера
- Провайдер сам проходит весь путь регистрации

---

## 5. РИСКИ И ПРОБЛЕМЫ

### 5.1. Технические риски

#### ⚠️ **Риск 1: Миграция существующих данных**
- **Проблема**: Существующие провайдеры могут иметь активные контракты
- **Решение**: 
  - Создать миграцию данных: для каждого активного контракта создать `ProviderOfferAcceptance` с текущей версией оферты
  - Установить `accepted_at` = дата создания контракта
  - Сохранить историю контрактов в архивной таблице

#### ⚠️ **Риск 2: Зависимость от внешних API (VIES)**
- **Проблема**: VIES API может быть недоступен или медленно отвечать
- **Решение**:
  - Кэшировать результаты проверки VAT
  - Добавить фоновую задачу для повторной проверки
  - Разрешить ручную проверку админом
  - Показывать предупреждение, если проверка не прошла

#### ⚠️ **Риск 3: Валидация реквизитов для разных стран**
- **Проблема**: Нет единого API для проверки реквизитов всех стран
- **Решение**:
  - Начать с VIES для ЕС (бесплатно, официально)
  - Для РФ: использовать существующие сервисы (если есть)
  - Для других стран: показывать предупреждение о необходимости ручной проверки
  - Добавить возможность ручной проверки админом

#### ⚠️ **Риск 4: Версионирование оферты**
- **Проблема**: Что делать, если оферта обновляется, а провайдер уже принял старую версию?
- **Решение**:
  - При обновлении оферты уведомлять провайдеров за 15 дней
  - Требовать принятия новой версии для продолжения работы
  - Хранить историю принятых версий
  - Автоматически деактивировать старые акцепты при новой версии

### 5.2. Бизнес-риски

#### ⚠️ **Риск 5: Юридическая валидность электронного акцепта**
- **Проблема**: Нужно убедиться, что электронный акцепт юридически валиден
- **Решение**:
  - Сохранять IP-адрес и User-Agent при акцепте
  - Сохранять дату и время акцепта
  - Сохранять версию оферты
  - Добавить возможность экспорта данных для судебных разбирательств

#### ⚠️ **Риск 6: Side Letter для крупных клиентов**
- **Проблема**: Нужен процесс создания и управления Side Letter
- **Решение**:
  - Создать отдельную модель `SideLetter`
  - Добавить админку для управления Side Letter
  - Интегрировать с процессом биллинга (применение измененных условий)

#### ⚠️ **Риск 7: Региональные дополнения**
- **Проблема**: Нужно управлять региональными дополнениями для разных стран
- **Решение**:
  - Хранить региональные дополнения в JSON поле `PublicOffer.regional_addendums`
  - Автоматически применять дополнения в зависимости от страны провайдера
  - Показывать полный текст оферты с дополнениями при принятии

### 5.3. Риски интеграции

#### ⚠️ **Риск 8: Интеграция с биллингом**
- **Проблема**: Биллинг может зависеть от контрактов
- **Решение**:
  - Проверить все места, где используются контракты
  - Заменить проверку контракта на проверку акцепта оферты
  - Обновить логику начисления комиссии (брать из оферты или Side Letter)

#### ⚠️ **Риск 9: Интеграция с блокировками**
- **Проблема**: Блокировки могут зависеть от контрактов
- **Решение**:
  - Убрать проверку контракта из логики блокировок
  - Оставить только проверку акцепта оферты
  - Обновить условия блокировок

---

## 6. ПЛАН РЕАЛИЗАЦИИ (ПОШАГОВО)

### Этап 1: Подготовка (1-2 дня)
1. ✅ Создать план миграции (этот документ)
2. ✅ Создать диаграммы новых флоу
3. ✅ Обновить функциональный дизайн
4. ✅ Провести code review существующего кода контрактов

### Этап 2: Создание новых моделей (2-3 дня)
1. Создать модель `PublicOffer`
2. Создать модель `ProviderOfferAcceptance`
3. Создать модель `SideLetter`
4. Расширить модель `Provider` новыми полями реквизитов
5. Создать миграции

### Этап 3: Создание сервисов (2-3 дня)
1. Создать `VATVerificationService` (интеграция с VIES API)
2. Создать `OfferGeneratorService` (генерация персонализированной оферты)
3. Создать утилиты для валидации реквизитов

### Этап 4: Обновление API (3-4 дня)
1. Создать API endpoints для принятия оферты
2. Создать API endpoints для проверки VAT
3. Обновить API регистрации провайдера
4. Создать API endpoints для управления офертами (админка)

### Этап 5: Обновление процессов (3-4 дня)
1. Обновить процесс регистрации провайдера
2. Убрать создание контракта при одобрении заявки
3. Добавить принятие оферты в процесс регистрации
4. Обновить процесс активации провайдера

### Этап 6: Миграция данных (1-2 дня)
1. Создать скрипт миграции существующих контрактов
2. Создать архивную таблицу для старых контрактов
3. Запустить миграцию данных
4. Проверить корректность миграции

### Этап 7: Тестирование (3-4 дня)
1. Протестировать новый процесс регистрации
2. Протестировать проверку VAT через VIES
3. Протестировать принятие оферты
4. Протестировать миграцию данных
5. Протестировать интеграцию с биллингом

### Этап 8: Удаление старого кода (1-2 дня)
1. Удалить модели контрактов
2. Удалить API endpoints контрактов
3. Удалить админку контрактов
4. Удалить диаграммы контрактов
5. Обновить документацию

### Этап 9: Деплой (1 день)
1. Создать резервную копию БД
2. Запустить миграции
3. Запустить миграцию данных
4. Проверить работоспособность системы
5. Мониторинг после деплоя

---

## 7. ВОПРОСЫ ДЛЯ УТОЧНЕНИЯ

1. **Нужна ли ручная проверка заявок провайдеров?**
   - Сейчас есть процесс одобрения заявки системным админом
   - В новой модели провайдер сам проходит весь путь
   - Нужно ли оставить модерацию или сделать полностью автоматическим?

2. **Как обрабатывать обновления оферты?**
   - Требовать ли принятия новой версии для всех провайдеров?
   - Или только для новых провайдеров?
   - Как обрабатывать провайдеров, которые не приняли новую версию?

3. **Интеграция с реестрами для РФ/УА:**
   - Есть ли доступ к API для проверки ИНН/ОГРН?
   - Или делать только валидацию формата?

4. **Side Letter:**
   - Кто может создавать Side Letter? (только системный админ?)
   - Нужна ли автоматизация применения условий из Side Letter в биллинге?

5. **Комиссия:**
   - Где хранить размер комиссии? (в оферте по умолчанию 5%, в Side Letter может быть изменена)
   - Как применять комиссию из Side Letter в биллинге?

---

## 8. ПРИМЕЧАНИЯ

- Все изменения должны быть обратно совместимыми на этапе миграции
- Нужно сохранить историю контрактов для аудита
- Все пользовательские сообщения должны быть на английском языке с поддержкой мультиязычности
- Все комментарии в коде должны быть на русском языке
- Все названия функций, переменных, классов должны быть на английском языке

