# План миграции на модель Публичной Оферты

> **⚠️ ПРИМЕЧАНИЕ**: Этот документ описывает исторический план миграции. Миграция завершена, и модели `PublicOffer`, `RegionalAddendum`, `ProviderOfferAcceptance` были заменены на универсальные модели из приложения `legal`: `LegalDocument`, `DocumentAcceptance`, `LegalDocumentType`, `DocumentTranslation`, `CountryLegalConfig`.

## 1. ЧТО УДАЛИТЬ (Очистка кода)

### 1.1. Модели и поля, которые больше не нужны

#### Модель `Contract` (❌ УДАЛИТЬ ПОСЛЕ МИГРАЦИИ)

- ✅ **Важно**: База почти пуста, нет платежей, нет активных контрактов → можно удалить Contract сразу после миграции биллинга
  
- ✅ **План миграции Contract**:
  1. **Этап 1**: Создать новые модели (`PublicOffer`, `ProviderOfferAcceptance`, `ProviderSpecialTerms`)
  2. **Этап 2**: Обновить биллинг для работы с офертами вместо контрактов (см. Этап 6)
  3. **Этап 3**: Удалить модель `Contract` и все связанные модели (см. Этап 9)

- ❌ **Удалить после миграции**: Все модели контрактов
  - `Contract` - основная модель контрактов
  - `ContractApprovalHistory` - история согласования контрактов (заменить на историю оферт)
  - `ContractCommission` - комиссии по контрактам (заменить на `ProviderSpecialTerms`)
  - `ContractDiscount` - скидки по контрактам (заменить на `ProviderSpecialTerms`)

- 📝 **Примечание**: 
  - `PaymentHistory` нужно переделать: заменить FK `contract` → `provider` (или `offer_acceptance`)
  - `Invoice` уже связан с `Provider` (не с Contract) → не требует изменений
  - Contract можно удалить сразу после обновления биллинга и PaymentHistory (база пуста)

#### Поля в `Provider`
- ❌ **Удалить**: Связь с контрактами через `related_name='contracts'`
- ⚠️ **Оставить**: `tax_id`, `registration_number` (будут расширены)

#### Процесс создания контрактов
- ❌ **Удалить**: 
  - API endpoints для создания/редактирования контрактов
  - Админка для контрактов (`ContractAdmin`)
  - Сериализаторы контрактов (`ContractSerializer`, `ContractCreateSerializer`)
  - Диаграммы: `5. PROVIDER_CONTRACT_CREATION.puml`, `6. PROVIDER_CONTRACT_APPROVAL.puml`
  - Сигналы, связанные с контрактами

#### Статусы контрактов
- ❌ **Удалить**: Все статусы контрактов (`draft`, `pending_approval`, `active`, `rejected`, `suspended`, `terminated`)
- ❌ **Удалить**: Поля `status`, `submitted_for_approval_at`, `approved_by`, `approved_at`, `rejection_reason` из `Contract`

### 1.2. Процессы, которые больше не нужны

#### Ручное создание контрактов
- ❌ **Удалить**: Процесс создания контракта биллинг-менеджером
- ❌ **Удалить**: Процесс согласования контракта системным админом
- ❌ **Удалить**: Процесс отправки оферты провайдеру на подпись

#### Проверка контрактов
- ❌ **Удалить**: Логику проверки наличия активного контракта перед выводом услуг на UI
- ❌ **Удалить**: Блокировки, связанные с отсутствием контракта

### 1.3. Файлы для удаления

- `PetsCare/billing/models.py`: классы `Contract`, `ContractApprovalHistory`, `ContractCommission`, `ContractDiscount`
- `PetsCare/billing/admin.py`: классы `ContractAdmin`, `ContractApprovalHistoryAdmin`
- `PetsCare/billing/serializers.py`: классы `ContractSerializer`, `ContractCreateSerializer`, `ContractApprovalHistorySerializer`
- `PetsCare/billing/api_views.py`: `ContractViewSet`
- `PetsCare/providers/docs/diagrams/`: `5. PROVIDER_CONTRACT_CREATION.puml`, `6. PROVIDER_CONTRACT_APPROVAL.puml`

---

## 2. ЧТО ДОБАВИТЬ

### 2.0. Новые зависимости

#### Библиотека `django-countries`
- **Назначение**: Работа со странами по стандарту ISO 3166-1 alpha-2
- **Использование**: Поле `CountryField` в модели `Provider`
- **Преимущества**: 
  - Все страны уже включены (не нужно создавать модель Country)
  - Поддержка флагов, названий на разных языках
  - Можно фильтровать: `Provider.objects.filter(country='DE')`
- **Установка**: `pip install django-countries`
- **Настройка**: Добавить `'django_countries'` в `INSTALLED_APPS`

### 2.1. Новые модели

#### Модель `PublicOffer` (замена `Contract`)

**Назначение**: Единая публичная оферта для всех провайдеров. Хранит текст оферты с переменными и базовые условия комиссии.

**Основные поля**:
- `version` - версия оферты (1.0, 1.1, etc.)
- `title` - название оферты
- `content` - текст оферты с переменными (например, `{{commission_percent}}`)
- `commission_percent` - процент комиссии (хранится в модели, подставляется в текст через `{{commission_percent}}`)
- `payment_deferral_days` - дни отсрочки платежа
- `invoice_period_days` - дни для генерации инвойса
- `effective_date` - дата вступления в силу
- `is_active` - активна ли оферта
- `change_notification_days` - дни уведомления об изменениях (минимум 90 дней для изменения процента)
- `notification_sent_at` - когда было отправлено уведомление
- `regional_addendums` - связь ManyToMany с региональными дополнениями

**Важно**: 
- Процент комиссии хранится в модели (`commission_percent`) и подставляется в текст оферты через переменную `{{commission_percent}}`
- В тексте оферты (п. 1.3) используется переменная: "вознаграждение в размере {{commission_percent}} от стоимости"
- При генерации персонализированной оферты переменная заменяется на значение из модели (например, 5%)
- Разные версии оферты могут иметь разные проценты (например, версия 1.0 - 5%, версия 1.1 - 3%)
- **КРИТИЧНО**: При изменении процента комиссии - обязательное уведомление всех провайдеров за 3 месяца (90 дней) до вступления в силу

#### Модель `RegionalAddendum` (региональные дополнения)

**Назначение**: Региональные дополнения к публичной оферте для конкретных стран/регионов (ЕС, РФ, УА и т.д.).

**Основные поля**:
- `region_code` - код региона (EU, RU, UA, US, etc.)
- `addendum_type` - тип дополнения (tax, data_protection, fiscal, consumer_protection, other)
- `region_name` - название региона
- `version` - версия дополнения
- `title` - заголовок дополнения
- `content` - текст дополнения (может содержать переменные из основной оферты)
- `variables` - JSON поле для дополнительных переменных (например, `{"vat_rate": "20", "fiscal_law": "54-ФЗ"}`)
- `effective_date` - дата вступления в силу
- `is_active` - активно ли дополнение

**Связь**: ManyToMany с `PublicOffer` (одна оферта может иметь несколько региональных дополнений)

#### Модель `ProviderOfferAcceptance` (акцепт оферты)

**Назначение**: Запись о принятии публичной оферты провайдером. Сохраняет информацию о принятой оферте и региональных дополнениях.

**Основные поля**:
- `provider` - FK на Provider
- `offer` - FK на PublicOffer
- `accepted_at` - дата и время принятия
- `accepted_by` - FK на User (кто принял)
- `ip_address` - IP-адрес (опционально, для юридической защиты и аудита)
- `user_agent` - User-Agent (опционально, для аудита)
- `is_active` - активен ли акцепт (если оферта обновлена, старые акцепты неактивны)
- `accepted_addendums` - ManyToMany с RegionalAddendum (какие региональные дополнения были приняты)

#### Модель `ProviderSpecialTerms` (специальные условия для провайдера - ОПЦИОНАЛЬНО)

**Назначение**: Специальные условия для конкретного провайдера, которые переопределяют стандартные условия из оферты.

**Логика**:
- Если у провайдера НЕТ `ProviderSpecialTerms` → используются стандартные условия из оферты (комиссия = `offer.commission_percent`)
- Если у провайдера ЕСТЬ `ProviderSpecialTerms` → используются индивидуальные условия

**Основные поля**:
- `provider` - OneToOne с Provider
- `commission_type` - тип комиссии (percent, fixed, hybrid, tiered)
- `commission_percent` - процент комиссии (если тип percent или hybrid)
- `commission_fixed` - фиксированная сумма (если тип fixed или hybrid)
- `commission_min` - минимальная комиссия
- `commission_max` - максимальная комиссия
- `tiered_rates` - JSON поле для прогрессивной шкалы
- `payment_deferral_days` - отсрочка платежа (переопределяет стандартную)
- `debt_threshold` - порог долга для блокировки (переопределяет стандартный)
- `overdue_threshold_1/2/3` - пороги просрочки для блокировки (переопределяют стандартные)
- `volume_discount_enabled` - включены ли скидки на объем
- `volume_discount_rules` - JSON поле с правилами скидок
- `activity_bonus_enabled` - включены ли бонусы за активность
- `activity_bonus_rules` - JSON поле с правилами бонусов
- `is_active` - активны ли условия

**Использование**:
- Для крупных клиентов с особыми условиями
- Для провайдеров с дисконтом (меньше стандартного процента)
- Для провайдеров с фиксированной ставкой
- Для провайдеров с гибридной моделью
- Для провайдеров с прогрессивной шкалой

#### Модель `SideLetter` (доп. соглашения для крупных клиентов - бумажный документ)

**Назначение**: Бумажный документ для очень крупных клиентов (сети из 500+ салонов), который изменяет отдельные пункты публичной оферты.

**Основные поля**:
- `provider` - FK на Provider
- `offer` - FK на PublicOffer
- `modified_clauses` - JSON поле с измененными пунктами оферты
- `document` - FileField для загруженного подписанного документа (PDF)
- `signed_at` - дата подписания
- `created_by` - FK на User (кто создал)
- `is_active` - активен ли Side Letter
- `special_terms` - OneToOne с ProviderSpecialTerms (если Side Letter создает специальные условия)

### 2.2. Расширение модели `Provider`

#### Новые поля реквизитов

**Основной блок**:
- `country` - страна регистрации (главный триггер, dropdown)
  - Использовать `CountryField` из библиотеки `django-countries` (ISO 3166-1 alpha-2)
  - Не создавать модель Country - библиотека уже предоставляет все страны
  - Можно фильтровать: `Provider.objects.filter(country='DE')`
- `is_eu` - принадлежность к ЕС (системное поле, автоматически вычисляется)
  - BooleanField в БД для производительности и фильтрации
  - Автоматически вычисляется при сохранении из констант ЕС
  - Можно фильтровать: `Provider.objects.filter(is_eu=True)`
  - Можно комбинировать фильтры: `Provider.objects.filter(is_eu=True, vat_verified=True)`
  - Можно создать индекс для производительности: `indexes = [models.Index(fields=['country', 'is_eu'])]`
- `organization_type` - тип организации (ИП, ООО, Corp, LLC, etc.)
- `legal_name` - юридическое название (дублирует name, но для ясности)
- `director_name` - ФИО директора

**Юридический блок**:
- `registration_number` - регистрационный номер (уже есть)
- `structured_address` - юридический адрес (уже есть)

**Налоговый блок**:
- `tax_id` - Tax ID / ИНН (уже есть)
- `kpp` - КПП (только для РФ, ООО, скрывается для других стран)
- `is_vat_payer` - плательщик НДС? (чекбокс)
- `vat_number` - VAT Number (только для ЕС, если is_vat_payer=True)
- `vat_verified` - результат проверки через VIES
- `vat_verification_date` - дата проверки VAT

**Финансовые данные**:
- `invoice_currency` - FK на Currency (валюта счета)
- `iban` - IBAN (для ЕС/УА) или номер расчетного счета (для РФ)
- `swift_bic` - SWIFT / BIC
- `bank_name` - название банка

**Управление видимостью услуг**:
- `show_services` - показывать ли услуги на UI (кнопка в админке провайдера)
- `show_services_toggled_at` - когда было изменено состояние

### 2.3. Новые сервисы и утилиты

#### Утилиты для работы со странами (`utils/countries.py`)

**Назначение**: Константы и функции для определения принадлежности стран к региональным союзам.

**Содержимое**:
- Константы со списками стран:
  - `EU_COUNTRIES` - страны-члены Европейского Союза (27 стран)
  - `EEA_COUNTRIES` - страны EEA (EU + Исландия, Лихтенштейн, Норвегия)
  - `EFTA_COUNTRIES` - страны EFTA (Исландия, Лихтенштейн, Норвегия, Швейцария)
  - `EAEU_COUNTRIES` - страны ЕАЭС (Армения, Беларусь, Казахстан, Кыргызстан, Россия)
- Функции для проверки:
  - `is_eu_country(country_code)` - проверка принадлежности к ЕС
  - `is_eea_country(country_code)` - проверка принадлежности к EEA
  - `is_eaeu_country(country_code)` - проверка принадлежности к ЕАЭС
  - `get_region_code(country_code)` - получение кода региона для RegionalAddendum

**Примечание**: 
- Список стран ЕС меняется редко (последний раз - Brexit в 2020)
- Константы обновляются вручную при изменениях
- Источник данных: https://european-union.europa.eu/principles-countries-history/country-profiles_en
- Официального API ЕС для списка стран нет, поэтому используем константы

#### Сервис проверки VAT через VIES API

**Назначение**: Проверка VAT номеров через VIES API (ЕС).

**Функционал**:
- Проверка VAT номера через VIES API
- Возврат результата: валидность, название компании, адрес, дата запроса, ошибка (если есть)
- Кэширование результатов
- Обработка ошибок API

#### Сервис генерации оферты

**Назначение**: Генерация персонализированной оферты для провайдера.

**Функционал**:
- Получение основного текста оферты
- Определение региона провайдера (по country)
- Получение активных региональных дополнений для этого региона
- Объединение основного текста + региональные дополнения
- Подстановка данных провайдера (название, адрес, директор и т.д.)
- Подстановка переменных ({{commission_percent}}, {{payment_deferral_days}}, etc.)

### 2.4. Новые API endpoints

#### Для провайдеров
- `POST /api/providers/{id}/accept-offer/` - принятие публичной оферты
- `GET /api/providers/{id}/offer/` - получение текста оферты для провайдера
- `POST /api/providers/{id}/verify-vat/` - проверка VAT номера через VIES

#### Для админов
- `GET /api/admin/offers/` - список публичных оферт
- `POST /api/admin/offers/` - создание новой версии оферты
- `POST /api/admin/providers/{id}/side-letter/` - создание Side Letter для провайдера

### 2.5. Новые процессы

#### Процесс регистрации провайдера (новый флоу)

**Роли в системе:**
- **Owner (Владелец/Главный аккаунт)**: Первый пользователь, который создает профиль организации. Именно он заполняет реквизиты и принимает оферту от имени организации. Автоматически становится `provider_admin` с полными правами.
- **Admin/Staff (Сотрудники)**: Пользователи, которых Owner приглашает позже для управления расписанием или услугами. Они не принимают оферту, так как действуют от имени уже зарегистрированной организации. Приглашение Admin/Staff - это отдельная функциональность (не в рамках текущей миграции).

**УПРОЩЕННЫЙ процесс регистрации:**
1. Регистрация пользователя (Email + Пароль) → **Owner**
2. Создание заявки провайдера (`ProviderForm`) - **ВСЕ ОБЯЗАТЕЛЬНО**:
   - Базовые данные организации (название, email, телефон, адрес)
   - **РЕКВИЗИТЫ** (обязательно!):
     * Tax ID / ИНН
     * Registration Number
     * Country (страна регистрации)
     * Invoice Currency (валюта счета)
   - **ПРИНЯТИЕ ОФЕРТЫ** (обязательно!):
     * offer_accepted = True
     * offer_accepted_at (время принятия)
     * offer_accepted_ip (IP адрес)
     * offer_accepted_user_agent (User Agent)
   - БЕЗ РЕКВИЗИТОВ И ОФЕРТЫ - ЗАЯВКА НЕ ПРИНИМАЕТСЯ!
3. **АВТОМАТИЧЕСКОЕ создание Provider** (через сигнал post_save):
   - Создается Provider с реквизитами из заявки
   - Копируются реквизиты из ProviderForm в Provider
   - Создается ProviderOfferAcceptance (если оферта принята)
   - Создается ProviderAdmin (связь Owner → Provider)
   - Owner получает роль `provider_admin`
   - Owner получает доступ к админке (is_staff=True)
   - **Автоматически назначается биллинг-менеджер** (первый доступный)
   - **ПРОВАЙДЕР СРАЗУ АКТИВИРУЕТСЯ** (activation_status='active', is_active=True) если реквизиты заполнены и оферта принята!

**Важно:**
- Owner может быть ИП (сам является провайдером)
- Owner может приглашать других пользователей на роль `provider_admin` или `staff` (после регистрации, через систему ролей)
- Поле "email админа провайдера" при регистрации **НЕ требуется** - Owner автоматически становится админом

#### Процесс проверки VAT (для ЕС)
1. Провайдер выбирает страну (ЕС)
2. Система помечает `is_eu = True`
3. Провайдер ставит галочку "Я плательщик НДС"
4. Провайдер вводит VAT Number
5. Система проверяет через VIES API
6. Если валидно - ставит `vat_verified = True`, `vat_exempt = True`
7. Если невалидно - показывает ошибку, НДС начисляется

---

## 3. ДИАГРАММЫ ФЛОУ (PlantUML)

### 3.1. Новые диаграммы для создания

#### `7. PROVIDER_REGISTRATION_WITH_OFFER.puml`
- Регистрация пользователя
- Создание организации
- Заполнение реквизитов
- Принятие публичной оферты
- Активация провайдера

#### `8. VAT_VERIFICATION_FLOW.puml`
- Выбор страны (ЕС)
- Ввод VAT Number
- Проверка через VIES API
- Обработка результата

#### `9. SIDE_LETTER_CREATION.puml`
- Создание Side Letter админом
- Привязка к провайдеру
- Загрузка документа
- Активация

### 3.2. Диаграммы для обновления

#### `1. PROVIDER_FORM_CREATION_SEQUENCE.puml`
- Добавить шаг принятия оферты
- Добавить шаг проверки VAT (для ЕС)

#### `2. PROVIDER_FORM_APPROVAL.puml`
- Убрать создание контракта
- Оставить только создание Provider и назначение биллинг-менеджера

#### `3. PROVIDER_REQUISITES_FILLING.puml`
- Расширить поля реквизитов
- Добавить проверку VAT (для ЕС)
- Добавить валидацию через реестры

---

## 4. ФОРМУЛИРОВКИ ДЛЯ ФУНКЦИОНАЛЬНОГО ДИЗАЙНА

### 4.1. Что вычистить из ФД

#### Раздел "Контракты"
- ❌ Удалить полностью раздел "Создание контрактов"
- ❌ Удалить раздел "Согласование контрактов"
- ❌ Удалить раздел "Отправка оферты провайдеру"
- ❌ Удалить раздел "Принятие оферты провайдером"
- ❌ Удалить статусы контрактов (`draft`, `pending_approval`, `active`, etc.)
- ❌ Удалить процесс создания контракта биллинг-менеджером
- ❌ Удалить процесс согласования контракта системным админом

#### Раздел "Провайдеры"
- ❌ Удалить проверку наличия активного контракта перед выводом услуг
- ❌ Удалить блокировки, связанные с отсутствием контракта
- ⚠️ Оставить процесс активации провайдера (но без привязки к контракту)

### 4.2. Что добавить/изменить в ФД

#### Новый раздел "Публичная оферта"

**4.2.1. Управление публичной офертой (для системных админов)**
- Создание новой версии оферты
- Редактирование текста оферты
- Установка даты вступления в силу
- Деактивация старых версий
- Привязка региональных дополнений к оферте (ManyToMany)

**4.2.1.1. Управление региональными дополнениями (для системных админов)**
- Создание региональных дополнений для разных регионов (ЕС, РФ, УА и т.д.)
- Выбор типа дополнения (tax, data_protection, fiscal, consumer_protection, other)
- Версионирование региональных дополнений
- Редактирование текста дополнений (с переменными типа `{{commission_percent}}`, `{{vat_rate}}`, etc.)
- Установка переменных дополнения (JSON поле `variables`)
- Установка даты вступления в силу
- Деактивация старых версий
- Привязка дополнений к офертам (ManyToMany)

**4.2.2. Принятие оферты провайдером (Owner)**

**Кто принимает оферту:**
- Оферту принимает **Owner (Владелец/Главный аккаунт)** - первый пользователь, который создал профиль организации
- Owner принимает оферту от имени организации (юридического лица или ИП)
- Owner автоматически становится `provider_admin` после принятия оферты
- Admin/Staff (приглашенные позже) не принимают оферту - они действуют от имени уже зарегистрированной организации

**Процесс принятия:**
- Автоматическая генерация персонализированной оферты (с подстановкой переменных из модели)
- Подстановка переменных в текст оферты: `{{commission_percent}}`, `{{payment_deferral_days}}`, etc.
- Автоматическое определение применимых региональных дополнений (по стране провайдера)
- Подстановка переменных в региональные дополнения (из основной оферты и из дополнения)
- Отображение полного текста оферты (основной текст + региональные дополнения) в личном кабинете
- **Чекбокс с текстом**: "Нажимая кнопку, вы подтверждаете, что имеете полномочия действовать от имени организации и принимаете условия Оферты"
- Сохранение акцепта с IP-адресом и User-Agent для аудита
- Сохранение информации о принятых региональных дополнениях
- Сохранение `accepted_by` = Owner (создатель заявки)
- Автоматическая активация провайдера после принятия оферты
- Owner автоматически получает роль `provider_admin` с полными правами

**4.2.2.1. Управление видимостью услуг провайдером**
- Кнопка "Показывать услуги" в админке провайдера
- Провайдер сам определяет готовность наполнения точек (услуги, работники)
- Провайдер сам управляет видимостью услуг на UI
- Услуги показываются на UI только если: `show_services = True` И провайдер не в блоке
- При изменении `show_services` сохраняется `show_services_toggled_at`

**4.2.3. Специальные условия для провайдеров (ProviderSpecialTerms)**
- Создание специальных условий для избранных провайдеров
- Типы комиссии:
  - **Процентная** (стандарт): 5% от стоимости записи
  - **Фиксированная**: например, 1000 руб. за каждую запись
  - **Гибридная**: фикс + процент (например, 500 руб. + 3%)
  - **Прогрессивная**: чем больше объем, тем меньше процент
    (0-10,000 руб. → 10%, 10,000-50,000 руб. → 8%, 50,000+ руб. → 5%)
- Минимальная/максимальная комиссия (для процентной модели)
- Переопределение отсрочки платежа
- Переопределение порогов блокировки
- Скидки на объем (опционально)
- Бонусы за активность (опционально)

**4.2.3.1. Side Letter (доп. соглашения - бумажный документ)**
- Создание Side Letter для очень крупных клиентов (сети из 500+ салонов)
- Указание измененных пунктов оферты в JSON
- Загрузка подписанного документа (PDF)
- Автоматическое создание `ProviderSpecialTerms` из Side Letter
- Привязка к провайдеру
- Активация/деактивация Side Letter

#### Обновление раздела "Реквизиты провайдера"

**4.2.4. Расширенные реквизиты**
- Выбор страны регистрации (главный триггер)
  - Использовать `CountryField` из библиотеки `django-countries`
  - Dropdown со всеми странами (ISO 3166-1 alpha-2)
  - Можно фильтровать провайдеров по стране: `Provider.objects.filter(country='DE')`
  - Можно фильтровать по нескольким странам: `Provider.objects.filter(country__in=['DE', 'FR', 'IT'])`
- Автоматическое определение принадлежности к ЕС (`is_eu`)
  - Поле `is_eu` автоматически вычисляется при сохранении из констант ЕС (`utils/countries.py`)
  - Можно фильтровать провайдеров по ЕС: `Provider.objects.filter(is_eu=True)`
  - Можно создавать индексы для производительности фильтрации
  - Можно комбинировать фильтры: `Provider.objects.filter(is_eu=True, vat_verified=True)`
- Выбор типа организации (ИП, ООО, Corp, LLC, etc.)
- Ввод ФИО директора
- Ввод регистрационного номера (с валидацией через реестры)
- Ввод Tax ID / ИНН (с валидацией через реестры)
- КПП (только для РФ, ООО) - скрывается для других стран
- Чекбокс "Я плательщик НДС"
- VAT Number (только для ЕС, если плательщик НДС) - с проверкой через VIES
- Валюта счета
- IBAN (для ЕС/УА) или номер расчетного счета (для РФ)
- SWIFT / BIC
- Название банка

**4.2.5. Валидация реквизитов**
- Проверка VAT Number через VIES API (для ЕС)
- Проверка ИНН через реестры (для РФ) - если есть API
- Проверка регистрационного номера через реестры (для разных стран)
- Автоматическое скрытие/показ полей в зависимости от страны
- Валидация формата IBAN
- Валидация формата SWIFT/BIC

#### Обновление раздела "Регистрация провайдера"

**4.2.6. Новый процесс регистрации**

**Роли:**
- **Owner (Владелец)**: Создает заявку, заполняет реквизиты, принимает оферту, автоматически становится `provider_admin`
- **Admin/Staff**: Приглашаются Owner'ом позже для управления расписанием/услугами (отдельная функциональность, не в рамках текущей миграции)

**УПРОЩЕННЫЙ процесс:**
1. Регистрация пользователя (Email + Пароль) → **Owner**
2. Создание заявки провайдера (`ProviderForm`) - **ВСЕ ОБЯЗАТЕЛЬНО**:
   - Базовые данные организации
   - **РЕКВИЗИТЫ** (обязательно!): Tax ID, Registration Number, Country, Invoice Currency
   - **ПРИНЯТИЕ ОФЕРТЫ** (обязательно!): offer_accepted=True с метаданными
   - БЕЗ РЕКВИЗИТОВ И ОФЕРТЫ - ЗАЯВКА НЕ ПРИНИМАЕТСЯ!
3. **АВТОМАТИЧЕСКОЕ создание Provider** (через сигнал post_save):
   - Создается Provider с реквизитами из заявки
   - Создается ProviderOfferAcceptance
   - Создается ProviderAdmin
   - Owner получает роль `provider_admin` и доступ к админке
   - **Автоматически назначается биллинг-менеджер** (первый доступный)
   - **ПРОВАЙДЕР СРАЗУ АКТИВИРУЕТСЯ** если реквизиты и оферта заполнены!

**Важно:**
- Owner может быть ИП (сам является провайдером)
- Owner может приглашать других пользователей на роль `provider_admin` или `staff` (после регистрации)
- Поле "email админа провайдера" при регистрации **НЕ требуется** - Owner автоматически становится админом

**4.2.7. Упрощение процесса регистрации** ✅ ВЫПОЛНЕНО
- ✅ Убрано одобрение системным админом - провайдер создается автоматически при создании заявки
- ✅ Убрано создание контракта (старая система удалена)
- ✅ Реквизиты и принятие оферты обязательны в заявке
- ✅ Автоматическое назначение биллинг-менеджера (первый доступный)
- ✅ Провайдер сразу активируется при создании заявки (если реквизиты и оферта заполнены)

---

## 5. РИСКИ И ПРОБЛЕМЫ

### 5.1. Технические риски

#### ⚠️ **Риск 1: Миграция существующих данных**
- **Проблема**: Существующие провайдеры могут иметь активные контракты
- **Решение**: 
  - Создать миграцию данных: для каждого активного контракта создать `ProviderOfferAcceptance` с текущей версией оферты
  - Установить `accepted_at` = дата создания контракта
  - Сохранить историю контрактов в архивной таблице

#### ⚠️ **Риск 2: Зависимость от внешних API (VIES)**
- **Проблема**: VIES API может быть недоступен или медленно отвечать
- **Решение**:
  - Кэшировать результаты проверки VAT
  - Добавить фоновую задачу для повторной проверки
  - Разрешить ручную проверку админом
  - Показывать предупреждение, если проверка не прошла

#### ⚠️ **Риск 3: Валидация реквизитов для разных стран**
- **Проблема**: Нет единого API для проверки реквизитов всех стран
- **Решение**:
  - Начать с VIES для ЕС (бесплатно, официально)
  - Для РФ: использовать существующие сервисы (если есть)
  - Для других стран: показывать предупреждение о необходимости ручной проверки
  - Добавить возможность ручной проверки админом

#### ⚠️ **Риск 4: Версионирование оферты**
- **Проблема**: Что делать, если оферта обновляется, а провайдер уже принял старую версию?
- **Решение**:
  - При обновлении оферты уведомлять провайдеров за 15 дней
  - Требовать принятия новой версии для продолжения работы
  - Хранить историю принятых версий
  - Автоматически деактивировать старые акцепты при новой версии

### 5.2. Бизнес-риски

#### ⚠️ **Риск 5: Юридическая валидность электронного акцепта**
- **Проблема**: Нужно убедиться, что электронный акцепт юридически валиден
- **Решение**:
  - Сохранять IP-адрес и User-Agent при акцепте (опционально, можно отключить если не требуется)
  - Сохранять дату и время акцепта (обязательно)
  - Сохранять версию оферты (обязательно)
  - Сохранять информацию о принятых региональных дополнениях (обязательно)
  - Добавить возможность экспорта данных для судебных разбирательств

#### ⚠️ **Риск 6: Side Letter для крупных клиентов**
- **Проблема**: Нужен процесс создания и управления Side Letter
- **Решение**:
  - Создать отдельную модель `SideLetter`
  - Добавить админку для управления Side Letter
  - Интегрировать с процессом биллинга (применение измененных условий)

#### ⚠️ **Риск 7: Региональные дополнения**
- **Проблема**: Нужно управлять региональными дополнениями для разных стран
- **Решение**:
  - Создать отдельную модель `RegionalAddendum` для версионирования и управления
  - Связь ManyToMany между `PublicOffer` и `RegionalAddendum`
  - Автоматически определять применимые дополнения по стране провайдера (`provider.country`, `provider.is_eu`)
  - Показывать полный текст оферты (основной + региональные дополнения) при принятии
  - Сохранять информацию о принятых дополнениях в `ProviderOfferAcceptance`

### 5.3. Риски интеграции

#### ⚠️ **Риск 8: Интеграция с биллингом**
- **Проблема**: Биллинг может зависеть от контрактов
- **Решение**:
  - Проверить все места, где используются контракты
  - Заменить проверку контракта на проверку акцепта оферты
  - Обновить логику начисления комиссии:
    - Если у провайдера есть `ProviderSpecialTerms` → использовать их
    - Если нет → использовать `offer.commission_percent` (из модели, подставляется в текст как {{commission_percent}})
    - Side Letter может автоматически создавать `ProviderSpecialTerms`

#### ⚠️ **Риск 9: Интеграция с блокировками**
- **Проблема**: Блокировки могут зависеть от контрактов
- **Решение**:
  - Убрать проверку контракта из логики блокировок
  - Оставить только проверку акцепта оферты
  - Обновить условия блокировок

---

## 6. ЛОГИКА РАСЧЕТА ЗАДОЛЖЕННОСТИ И ПЛАТЕЖЕЙ

### 6.1. Как работает расчет задолженности без Contract

**Текущая логика (с Contract)**:
- `Contract.calculate_debt()` - рассчитывает долг на основе Invoice и PaymentHistory
- `Contract.get_max_overdue_days()` - получает максимальные дни просрочки
- Пороги блокировки хранятся в Contract (`debt_threshold`, `overdue_threshold_*`)

**Новая логика (с офертами)**:

#### Шаг 1: Определение активной оферты
- Находим активный `ProviderOfferAcceptance` для провайдера (`is_active=True`)
- Получаем `PublicOffer` из акцепта
- Если у провайдера есть `ProviderSpecialTerms` → используем их для переопределения условий

#### Шаг 2: Расчет комиссии за бронирования
- Для каждого завершенного бронирования:
  - Если есть `ProviderSpecialTerms` → используем `ProviderSpecialTerms.calculate_commission()`
  - Если нет → используем стандартный процент из `offer.commission_percent`
- Комиссия накапливается за период (месяц)

#### Шаг 3: Генерация Invoice
- В конце периода (месяца) создается `Invoice` для провайдера
- `Invoice` связан с `Provider` (не с Contract)
- `Invoice` содержит строки (`InvoiceLine`) с комиссией за каждое бронирование
- Сумма Invoice = сумма всех комиссий за период

#### Шаг 4: Расчет задолженности
- Задолженность = сумма всех неоплаченных Invoice для провайдера
- `Invoice.status` может быть: `draft`, `sent`, `paid`, `overdue`, `cancelled`
- Задолженность = сумма Invoice со статусом `sent` или `overdue`

#### Шаг 5: Проверка блокировок
- Пороги блокировки берутся из:
  - `ProviderSpecialTerms.debt_threshold` (если есть)
  - Или стандартные пороги из оферты (если нет специальных условий)
- Проверяем:
  - Если задолженность > `debt_threshold` → блокировка
  - Если Invoice просрочен на `overdue_threshold_1` дней → уведомление
  - Если Invoice просрочен на `overdue_threshold_2` дней → исключение из поиска
  - Если Invoice просрочен на `overdue_threshold_3` дней → полная блокировка

#### Шаг 6: PaymentHistory (история платежей)
- `PaymentHistory` связан с `Provider` (вместо Contract)
- Опционально: `PaymentHistory` может быть связан с `ProviderOfferAcceptance` для истории
- `PaymentHistory` создается на основе Invoice
- При оплате Invoice создается запись в `PaymentHistory` со статусом `paid`

### 6.2. Пример расчета задолженности

**Сценарий**:
- Провайдер принял оферту версии 1.0 (комиссия 5%)
- За январь было 10 бронирований по 1000 руб. каждое
- Комиссия за каждое: 1000 * 5% = 50 руб.
- Общая комиссия за январь: 10 * 50 = 500 руб.
- Создан Invoice на 500 руб. со статусом `sent`
- Провайдер не оплатил Invoice
- Задолженность = 500 руб.

**Если у провайдера есть ProviderSpecialTerms (комиссия 2%)**:
- Комиссия за каждое: 1000 * 2% = 20 руб.
- Общая комиссия за январь: 10 * 20 = 200 руб.
- Invoice на 200 руб.
- Задолженность = 200 руб.

### 6.3. Связь с региональными дополнениями

**Важно**: Региональные дополнения (`RegionalAddendum`) не влияют на расчет комиссии и задолженности. Они только добавляют юридические/технические требования к оферте (GDPR, 54-ФЗ, etc.).

**Расчет комиссии зависит только от**:
- `PublicOffer.commission_percent` (стандартный процент)
- `ProviderSpecialTerms` (если есть, переопределяет стандартный процент)

---

## 7. ПЛАН РЕАЛИЗАЦИИ (ПОШАГОВО)

### Этап 1: Подготовка (1-2 дня)
1. ✅ Создать план миграции (этот документ)
2. ✅ Создать диаграммы новых флоу
3. ✅ Обновить функциональный дизайн
4. ✅ Провести code review существующего кода контрактов

### Этап 2: Создание новых моделей (4-5 дней)
1. Установить библиотеку `django-countries` (`pip install django-countries`)
2. Добавить `'django_countries'` в `INSTALLED_APPS`
3. Создать утилиты `utils/countries.py` с константами ЕС, EEA, EAEU и функциями проверки
4. Создать модель `RegionalAddendum` (региональные дополнения)
5. Создать модель `PublicOffer` (связь ManyToMany с RegionalAddendum, базовые условия комиссии)
6. Создать модель `ProviderOfferAcceptance`
7. Создать модель `ProviderSpecialTerms` (специальные условия для провайдеров)
8. Создать модель `SideLetter` (бумажные документы для крупных клиентов)
9. Расширить модель `Provider` новыми полями реквизитов:
   - `country` (CountryField из django-countries)
   - `is_eu` (BooleanField, автоматически вычисляется в save() из utils/countries.py)
   - `is_eea`, `is_eaeu` (опционально, если нужны для фильтрации)
   - Остальные поля реквизитов (organization_type, director_name, kpp, vat_number, etc.)
10. Создать индексы для производительности фильтрации: `indexes = [models.Index(fields=['country', 'is_eu'])]`
11. Создать миграции

### Этап 3: Создание сервисов (2-3 дня)
1. Создать `VATVerificationService` (интеграция с VIES API)
2. Создать `OfferGeneratorService` (генерация персонализированной оферты)
3. Создать утилиты для валидации реквизитов

### Этап 4: Обновление API (3-4 дня)
1. Создать API endpoints для принятия оферты
2. Создать API endpoints для проверки VAT
3. Обновить API регистрации провайдера
4. Создать API endpoints для управления офертами (админка)

### Этап 5: Обновление процессов (3-4 дня)
1. Обновить процесс регистрации провайдера:
   - Owner создает заявку → одобрение системным админом → Owner заполняет реквизиты → Owner принимает оферту
   - Owner автоматически становится `provider_admin` после принятия оферты
   - Убрать поле "email админа провайдера" при регистрации (Owner = админ)
2. Убрать создание контракта при одобрении заявки
3. Добавить принятие оферты в процесс регистрации:
   - Отображение полного текста оферты (основной + региональные дополнения)
   - Чекбокс: "Нажимая кнопку, вы подтверждаете, что имеете полномочия действовать от имени организации и принимаете условия Оферты"
   - Сохранение `ProviderOfferAcceptance` (Owner = `accepted_by`)
4. **Обновить флоу отправки писем**:
   - ❌ Убрать отправку письма при активации провайдера (`send_provider_activation_email`)
   - ✅ Добавить отправку письма при принятии оферты (`send_offer_acceptance_email`)
   - Письмо должно отправляться:
     * Owner'у (создателю заявки, `admin_user.email`) - админу провайдера
     * На email провайдера (provider.email) - если отличается от email Owner'а
   - Письмо должно содержать:
     * Информацию о принятой оферте (версия, процент комиссии)
     * Ссылку на личный кабинет
     * Контакты биллинг-менеджера
5. Обновить процесс активации провайдера (автоматическая активация после принятия оферты)
6. **Примечание**: Приглашение Admin/Staff Owner'ом - это отдельная функциональность (не в рамках текущей миграции)

### Этап 6: Миграция биллинга (5-7 дней) ⚠️ КРИТИЧНО
1. **Обновить расчет комиссии**:
   - Заменить `contract.calculate_commission()` на логику с офертами
   - Использовать `ProviderSpecialTerms` если есть, иначе `offer.commission_percent`
2. **Обновить расчет задолженности**:
   - Заменить `contract.calculate_debt()` на расчет через Invoice
   - Задолженность = сумма неоплаченных Invoice для провайдера
3. **Обновить проверку блокировок**:
   - Заменить `contract.get_max_overdue_days()` на логику с офертами
   - Пороги блокировки брать из `ProviderSpecialTerms` или стандартные из оферты
4. **Обновить генерацию Invoice**:
   - Invoice уже связан с Provider (не требует изменений)
   - Использовать комиссию из оферты или ProviderSpecialTerms
5. **Обновить PaymentHistory**:
   - Заменить FK `contract` → `provider` (или `offer_acceptance`)
   - Обновить все места, где используется `payment_history.contract`
   - Обновить отображение: вместо `contract.number` использовать `offer.version` или `invoice.number`

### Этап 7: Миграция данных (1 день) ⚠️ БАЗА ПУСТА
1. Проверить, что в базе нет активных контрактов (база пуста)
2. Если есть тестовые данные - удалить их или мигрировать в оферты
3. Проверить корректность миграции

### Этап 8: Тестирование (4-5 дней)
1. Протестировать новый процесс регистрации
2. Протестировать проверку VAT через VIES
3. Протестировать принятие оферты
4. Протестировать отправку писем при принятии оферты (вместо активации)
5. Протестировать расчет долга через оферты
6. Протестировать проверку блокировок через оферты
7. Протестировать генерацию инвойсов с офертами
8. Протестировать миграцию данных
9. Протестировать интеграцию с биллингом (полный цикл)

### Этап 9: Удаление Contract (1 день) ⚠️ СРАЗУ ПОСЛЕ МИГРАЦИИ БИЛЛИНГА

**Важно**: База почти пуста, нет платежей, нет активных контрактов → можно удалить Contract сразу после обновления биллинга

#### Как будут работать платежи без Contract:

**Текущая структура**:
- `PaymentHistory` → FK на `Contract` (для истории платежей по договорам)
- `Invoice` → FK на `Provider` (уже не на Contract!)
- `Payment` → FK на `Booking` (платежи клиентов за бронирования)

**Новая структура**:
1. **PaymentHistory** (история платежей провайдеров за комиссию):
   - Заменить FK `contract` → `provider` (или `offer_acceptance`)
   - Платежи создаются на основе Invoice (который уже связан с Provider)

2. **Invoice** (уже связан с Provider, не с Contract):
   - Остается как есть: `provider = ForeignKey(Provider)`
   - Вместо `contract.number` используем `offer.version` или `invoice.number`

3. **Генерация платежей**:
   - Платежи генерируются на основе Invoice
   - Invoice связан с Provider
   - Provider связан с ProviderOfferAcceptance (активная оферта)
   - Комиссия берется из `offer.commission_percent` или `ProviderSpecialTerms`

**План удаления Contract**:

1. **Обновить PaymentHistory**:
   - Заменить FK `contract` → `provider` (или `offer_acceptance`)
   - Создать миграцию для замены FK
   - Обновить все места, где используется `payment_history.contract`
   - Обновить отображение: вместо `contract.number` использовать `offer.version` или `invoice.number`

2. **Проверить, что все использования Contract заменены**:
   - ✅ Биллинг работает с офертами
   - ✅ Инвойсы генерируются с офертами
   - ✅ Блокировки проверяются через оферты
   - ✅ PaymentHistory обновлен
   - ✅ Все тесты проходят
   
3. **Удалить модели контрактов**:
   - `Contract`
   - `ContractApprovalHistory`
   - `ContractCommission`
   - `ContractDiscount`
   
4. Удалить API endpoints контрактов
5. Удалить админку контрактов
6. Удалить диаграммы контрактов (или переместить в архив)
7. Обновить документацию

### Этап 10: Деплой (1 день)
1. Создать резервную копию БД
2. Запустить миграции
3. Запустить миграцию данных
4. Проверить работоспособность системы
5. Мониторинг после деплоя

---

## 8. ЛОГИКА ПРИМЕНЕНИЯ УСЛОВИЙ КОМИССИИ

### 8.1. Стандартные условия (из оферты)

**Процент комиссии хранится в модели и подставляется в текст оферты через переменную**:
- В тексте оферты (п. 1.3): "вознаграждение в размере {{commission_percent}} от стоимости оказанных услуг"
- При генерации персонализированной оферты переменная заменяется: `{{commission_percent}}` → `5%` (значение из `offer.commission_percent`)

**Особенности**:
- Это стандартное условие для всех провайдеров, принимающих эту версию оферты
- Процент хранится в модели `offer.commission_percent` (по умолчанию 5%)
- Разные версии оферты могут иметь разные проценты (например, версия 1.0 - 5%, версия 1.1 - 3%)
- **КРИТИЧНО**: При изменении процента комиссии - обязательное уведомление всех провайдеров за 3 месяца (90 дней) до вступления в силу

### 8.2. Специальные условия (опционально)

**`ProviderSpecialTerms` - опциональные условия только для избранных провайдеров**:
- Создаются только для тех провайдеров, которым нужны особые условия
- Переопределяют стандартные условия из оферты
- Если у провайдера нет `ProviderSpecialTerms` → используются стандартные условия из оферты

**Типы комиссии**:
- **Процентная** (стандарт): процент от стоимости записи
- **Фиксированная**: фиксированная сумма за каждую запись
- **Гибридная**: фикс + процент
- **Прогрессивная**: чем больше объем, тем меньше процент

### 8.3. Логика расчета комиссии

**Алгоритм**:
1. Получаем активную оферту, которую принял провайдер (`ProviderOfferAcceptance` с `is_active=True`)
2. Проверяем наличие `ProviderSpecialTerms` у провайдера
3. Если есть → используем `ProviderSpecialTerms.calculate_commission()`
4. Если нет → используем стандартный процент из оферты (`offer.commission_percent`)

### 8.4. Примеры

**Пример 1: Стандартный провайдер (нет специальных условий)**
- Оферта: `commission_percent = 5%` (из модели)
- Текст оферты (п. 1.3): "вознаграждение в размере {{commission_percent}} от стоимости"
- При генерации: "вознаграждение в размере 5% от стоимости"
- Нет `ProviderSpecialTerms`
- Комиссия: 5% от стоимости записи

**Пример 2: Провайдер с дисконтом (есть специальные условия)**
- Оферта: `commission_percent = 5%` (из модели)
- Текст оферты (п. 1.3): "вознаграждение в размере {{commission_percent}} от стоимости" → "вознаграждение в размере 5% от стоимости"
- `ProviderSpecialTerms`: `commission_percent = 2%`
- Комиссия: 2% от стоимости записи (переопределяет стандартные 5%)

**Пример 3: Провайдер с фиксированной ставкой**
- Оферта: `commission_percent = 5%` (из модели)
- Текст оферты (п. 1.3): "вознаграждение в размере {{commission_percent}} от стоимости" → "вознаграждение в размере 5% от стоимости"
- `ProviderSpecialTerms`: `commission_type = 'fixed'`, `commission_fixed = 1000`
- Комиссия: 1000 руб. за каждую запись (независимо от стоимости)

---

## 9. ВОПРОСЫ ДЛЯ УТОЧНЕНИЯ

1. **Нужна ли ручная проверка заявок провайдеров?** ✅ РЕШЕНО
   - Процесс одобрения заявки системным админом остается
   - После одобрения Owner заполняет реквизиты и принимает оферту
   - Owner автоматически становится `provider_admin` после принятия оферты

2. **Как обрабатывать обновления оферты?**
   - Требовать ли принятия новой версии для всех провайдеров?
   - Или только для новых провайдеров?
   - Как обрабатывать провайдеров, которые не приняли новую версию?

3. **Интеграция с реестрами для РФ/УА:**
   - Есть ли доступ к API для проверки ИНН/ОГРН?
   - Или делать только валидацию формата?

4. **Side Letter:**
   - Кто может создавать Side Letter? (только системный админ?)
   - Нужна ли автоматизация применения условий из Side Letter в биллинге?

5. **Комиссия:** ✅ РЕШЕНО
   - Где хранить размер комиссии?
     - В тексте оферты (п. 1.3): стандартный процент для всех (например, 5%)
     - В `ProviderSpecialTerms`: опциональные условия для избранных провайдеров
   - Как применять комиссию в биллинге?
     - Если у провайдера есть `ProviderSpecialTerms` → используем их
     - Если нет → используем стандартный процент из оферты (п. 1.3)
     - Side Letter может автоматически создавать `ProviderSpecialTerms`

6. **Роли Owner/Admin/Staff:** ✅ РЕШЕНО
   - Owner (Владелец) создает заявку, заполняет реквизиты, принимает оферту, автоматически становится `provider_admin`
   - Owner может быть ИП (сам является провайдером)
   - Owner может приглашать других пользователей на роль `provider_admin` или `staff` (после регистрации, через систему ролей)
   - Поле "email админа провайдера" при регистрации **НЕ требуется** - Owner автоматически становится админом
   - Приглашение Admin/Staff - это отдельная функциональность (не в рамках текущей миграции)

---

## 10. ПРИМЕЧАНИЯ

- Все изменения должны быть обратно совместимыми на этапе миграции
- Нужно сохранить историю контрактов для аудита
- Все пользовательские сообщения должны быть на английском языке с поддержкой мультиязычности
- Все комментарии в коде должны быть на русском языке
- Все названия функций, переменных, классов должны быть на английском языке
