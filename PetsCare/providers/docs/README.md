# Документация по провайдерам

Эта папка содержит документацию, связанную с функциональностью провайдеров услуг.

## Структура

```
docs/
├── diagrams/          # PlantUML диаграммы
│   ├── 1. PROVIDER_FORM_CREATION_SEQUENCE.puml
│   ├── 2. PROVIDER_FORM_APPROVAL.puml
│   ├── 3. PROVIDER_REQUISITES_FILLING.puml
│   ├── 4. PROVIDER_ACTIVATION.puml
│   ├── 5. PROVIDER_CONTRACT_CREATION.puml (⚠️ БУДЕТ УДАЛЕНА - заменена на оферту)
│   ├── 6. PROVIDER_CONTRACT_APPROVAL.puml (⚠️ БУДЕТ УДАЛЕНА - заменена на оферту)
│   ├── 7. PROVIDER_REGISTRATION_WITH_OFFER.puml (🆕 НОВАЯ)
│   ├── 8. VAT_VERIFICATION_FLOW.puml (🆕 НОВАЯ)
│   ├── 9. SIDE_LETTER_CREATION.puml (🆕 НОВАЯ)
│   ├── PROVIDER_ADMIN_ACCESS.puml
│   ├── PROVIDER_UI_DISPLAY_FLOW.puml
│   ├── PROVIDER_ACCESS_IMPLEMENTATION.puml
│   └── PROVIDER_ADMIN_ACCESS_AND_UI_FLOW.puml
├── analysis/          # Анализ и планы
│   └── PUBLIC_OFFER_MIGRATION_PLAN.md (🆕 План миграции на публичную оферту)
└── README.md          # Этот файл
```

## Диаграммы

### 1. PROVIDER_FORM_CREATION_SEQUENCE.puml
Диаграмма последовательности создания заявки провайдера услуг. Показывает процесс от заполнения формы (включая выбор категорий услуг уровня 0) до сохранения в базе данных.

### 2. PROVIDER_FORM_APPROVAL.puml
Диаграмма последовательности одобрения заявки провайдера и назначения биллинг-менеджера системным администратором. Показывает процесс одобрения, создания Provider, установки категорий услуг, назначения ролей, назначения биллинг-менеджера и отправки писем.

### 3. PROVIDER_REQUISITES_FILLING.puml
Диаграмма последовательности заполнения реквизитов провайдера (ИНН, регистрационный номер) биллинг-менеджером. Показывает процесс заполнения реквизитов и автоматическую установку статуса `activation_required`.

### 4. PROVIDER_ACTIVATION.puml
Диаграмма последовательности активации провайдера системным администратором. Показывает процесс активации провайдера после заполнения реквизитов (перевод из `activation_required` в `active`). После активации админ провайдера может создавать точки и услуги, но не может создавать работников и расписания до заключения контракта.

### 5. PROVIDER_CONTRACT_CREATION.puml ⚠️ УСТАРЕЛА
**Статус**: Будет удалена после миграции на публичную оферту.

Диаграмма последовательности создания контракта провайдера (старая модель). Показывает процесс создания контракта биллинг-менеджером, включая автоматическое наследование порогов блокировки и стандартных условий из типа контракта. Контракт создается со статусом `draft`.

**Заменена на**: `7. PROVIDER_REGISTRATION_WITH_OFFER.puml`

### 6. PROVIDER_CONTRACT_APPROVAL.puml ⚠️ УСТАРЕЛА
**Статус**: Будет удалена после миграции на публичную оферту.

Диаграмма последовательности одобрения контракта провайдера (старая модель). Показывает процесс отправки контракта на согласование биллинг-менеджером и одобрения системным администратором.

**Заменена на**: `7. PROVIDER_REGISTRATION_WITH_OFFER.puml`

### 7. PROVIDER_REGISTRATION_WITH_OFFER.puml 🆕 НОВАЯ
**Статус**: Актуальная диаграмма нового процесса регистрации.

Диаграмма последовательности регистрации провайдера с принятием публичной оферты. Показывает полный процесс:
1. Регистрация пользователя (Email + Пароль)
2. Создание организации (базовые данные)
3. Заполнение реквизитов (с валидацией через реестры)
4. Принятие публичной оферты (автоматическое создание акцепта)
5. Автоматическая активация провайдера

**См. также**: `PUBLIC_OFFER_MIGRATION_PLAN.md` для детального плана миграции.

### 8. VAT_VERIFICATION_FLOW.puml 🆕 НОВАЯ
**Статус**: Актуальная диаграмма проверки VAT номеров.

Диаграмма последовательности проверки VAT номера через VIES API (ЕС). Показывает процесс:
1. Выбор страны регистрации (ЕС)
2. Установка флага "Я плательщик НДС"
3. Ввод VAT Number
4. Проверка через VIES API (официальный шлюз Еврокомиссии)
5. Обработка результата (vat_verified, vat_exempt)

**Особенности**:
- Интеграция с бесплатным VIES API
- Обработка ошибок (недоступность API)
- Автоматическое определение Reverse Charge для ЕС

### 9. SIDE_LETTER_CREATION.puml 🆕 НОВАЯ
**Статус**: Актуальная диаграмма создания доп. соглашений.

Диаграмма последовательности создания Side Letter (дополнительного соглашения) для крупных клиентов. Показывает процесс:
1. Выбор провайдера системным админом
2. Создание Side Letter с указанием измененных пунктов оферты
3. Загрузка подписанного документа
4. Активация Side Letter
5. Применение условий в биллинге

**Использование**: Для крупных клиентов (сети из 500+ салонов), которым нужны особые условия (например, комиссия 5% вместо 10%).

### PROVIDER_ADMIN_ACCESS.puml
Диаграмма доступа администратора провайдера к моделям в Django Admin. Описывает права доступа к различным моделям в зависимости от статуса провайдера и наличия контракта.

### PROVIDER_UI_DISPLAY_FLOW.puml
Диаграмма логики отображения услуг провайдера на фронтенде. Показывает условия, при которых услуги провайдера отображаются конечным пользователям.

### PROVIDER_ACCESS_IMPLEMENTATION.puml
Детали реализации прав доступа для администратора провайдера. Описывает методы проверки прав, логику автозаполнения и проверки контрактов.

### PROVIDER_ADMIN_ACCESS_AND_UI_FLOW.puml
Объединенная диаграмма, включающая доступ администратора провайдера и логику отображения на UI.

## Миграция на публичную оферту

Система переходит с модели индивидуальных контрактов на модель публичной оферты. Подробный план миграции см. в `analysis/PUBLIC_OFFER_MIGRATION_PLAN.md`.

**Основные изменения**:
- ❌ Удаление модели `Contract` и связанных процессов
- ✅ Внедрение единой публичной оферты для всех провайдеров
- ✅ Автоматическое принятие оферты при регистрации
- ✅ Side Letter для крупных клиентов с особыми условиями
- ✅ Интеграция с VIES API для проверки VAT номеров (ЕС)
- ✅ Расширенные реквизиты провайдеров (страна, тип организации, VAT Number и т.д.)

**Новые диаграммы**:
- `7. PROVIDER_REGISTRATION_WITH_OFFER.puml` - новый процесс регистрации
- `8. VAT_VERIFICATION_FLOW.puml` - проверка VAT через VIES
- `9. SIDE_LETTER_CREATION.puml` - создание доп. соглашений

**Устаревшие диаграммы** (будут удалены):
- `5. PROVIDER_CONTRACT_CREATION.puml`
- `6. PROVIDER_CONTRACT_APPROVAL.puml`

## Как просматривать диаграммы

Диаграммы написаны в формате PlantUML. Для просмотра можно использовать:

1. **VS Code / Cursor**: Установите расширение "PlantUML"
2. **Онлайн**: http://www.plantuml.com/plantuml/uml/
3. **Локально**: Установите PlantUML и используйте команду `plantuml diagram.puml`

