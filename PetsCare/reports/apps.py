from django.apps import AppConfig

class ReportsConfig(AppConfig):
    """
    Конфигурация приложения для отчётов (Reports).
    Используется для регистрации приложения в проекте.
    """
    name = 'reports'
    verbose_name = 'Reports' 